# Payments

Amounts are in **dollars** as floating-point values (for example `19.99` means nineteen dollars and ninety-nine cents).

`apply_discount(amount, percent)` should return the discounted dollar amount.
